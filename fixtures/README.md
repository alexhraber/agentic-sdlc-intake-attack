# Fixtures

Fixtures in this repository are harmless placeholders. They are not malware samples and are not derived executable payloads.

Allowed fixture types:

- fake manifests;
- redacted metadata examples;
- dummy archive listings;
- hash table examples;
- policy test cases with inert filenames.

Do not replace these files with `core_fix_v2.zip`, `core_fix_v2.exe`, or any real suspicious artifact.
